{
  "name": "stop-list",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client && npm run setup --prefix server",
    "setup": "npm run setup --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm test --prefix server -- --run"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}